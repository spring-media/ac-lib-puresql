{
  "name": "@ac/ac-lib-puresql",
  "version": "2.0.5",
  "description": "forks puresql. SQL library for node.js inspired by Clojure's yesql",
  "engines": {
    "node": ">= 6.0.0"
  },
  "main": "index.js",
  "types": "index.d.ts",
  "typings": "index.d.ts",
  "scripts": {
    "test": "nyc mocha --recursive --inline-diff --exit",
    "watch": "mocha --watch --recursive --inline-diff",
    "watch-filter": "mocha --watch --recursive --inline-diff -g "
  },
  "author": "Christian Waltjen",
  "license": "MIT",
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-as-promised": "^5.3.0",
    "co": "^4.6.0",
    "mocha": "^7.1.1",
    "mysql2": "^2.1.0",
    "nyc": "^14.1.1"
  },
  "dependencies": {
    "@types/mysql2": "github:types/mysql2"
  }
}
